// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.29.0
// source: seed.sql

package gen

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const countLessons = `-- name: CountLessons :one
select count(*)::int from lessons
`

func (q *Queries) CountLessons(ctx context.Context) (int32, error) {
	row := q.db.QueryRow(ctx, countLessons)
	var column_1 int32
	err := row.Scan(&column_1)
	return column_1, err
}

const countRooms = `-- name: CountRooms :one
select count(*)::int from rooms
`

func (q *Queries) CountRooms(ctx context.Context) (int32, error) {
	row := q.db.QueryRow(ctx, countRooms)
	var column_1 int32
	err := row.Scan(&column_1)
	return column_1, err
}

const insertBuilding = `-- name: InsertBuilding :one
insert into buildings (id, code, name, timezone) values ($1, $2, $3, $4)
returning id, code, name, timezone
`

type InsertBuildingParams struct {
	ID       pgtype.UUID
	Code     string
	Name     string
	Timezone string
}

func (q *Queries) InsertBuilding(ctx context.Context, arg InsertBuildingParams) (Building, error) {
	row := q.db.QueryRow(ctx, insertBuilding,
		arg.ID,
		arg.Code,
		arg.Name,
		arg.Timezone,
	)
	var i Building
	err := row.Scan(
		&i.ID,
		&i.Code,
		&i.Name,
		&i.Timezone,
	)
	return i, err
}

const insertSemester = `-- name: InsertSemester :one
insert into semesters (id, name, starts_on, ends_on, week1_parity)
values ($1, $2, $3, $4, $5)
returning id, name, starts_on, ends_on, week1_parity
`

type InsertSemesterParams struct {
	ID          pgtype.UUID
	Name        string
	StartsOn    pgtype.Date
	EndsOn      pgtype.Date
	Week1Parity string
}

func (q *Queries) InsertSemester(ctx context.Context, arg InsertSemesterParams) (Semester, error) {
	row := q.db.QueryRow(ctx, insertSemester,
		arg.ID,
		arg.Name,
		arg.StartsOn,
		arg.EndsOn,
		arg.Week1Parity,
	)
	var i Semester
	err := row.Scan(
		&i.ID,
		&i.Name,
		&i.StartsOn,
		&i.EndsOn,
		&i.Week1Parity,
	)
	return i, err
}

const truncateAll = `-- name: TruncateAll :exec

truncate table
  announcements,
  session_override_groups,
  session_overrides,
  lesson_groups,
  lessons,
  time_slots,
  semesters,
  courses,
  student_groups,
  teachers,
  rooms,
  floors,
  buildings
restart identity cascade
`

// Bulk inserts used by cmd/seed. They are `:batchexec`, so sqlc emits pgx
// batches: seeding the whole building costs a handful of round trips rather
// than one per row, while every column keeps its real type (enums included).
func (q *Queries) TruncateAll(ctx context.Context) error {
	_, err := q.db.Exec(ctx, truncateAll)
	return err
}
