packages:
  - "apps/*"
  - "packages/*"

# pnpm 10 blocks lifecycle scripts by default. esbuild (tsx / vitest / Next.js)
# needs its postinstall to place the platform binary.
onlyBuiltDependencies:
  - esbuild
